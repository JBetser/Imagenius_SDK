#include "stdafx.h"
#include "MainWindow.h"

LRESULT CMainWindow::OnToolsResize(WORD wCode, WORD wId, HWND hWnd, BOOL &bHandled)
{
	bHandled = TRUE;
	return TRUE;
}

LRESULT CMainWindow::OnToolsRotate(WORD wCode, WORD wId, HWND hWnd, BOOL &bHandled)
{
	bHandled = TRUE;
	return TRUE;
}

LRESULT CMainWindow::OnToolsBrightness(WORD wCode, WORD wId, HWND hWnd, BOOL &bHandled)
{
	bHandled = TRUE;
	return TRUE;
}

LRESULT CMainWindow::OnToolsContrast(WORD wCode, WORD wId, HWND hWnd, BOOL &bHandled)
{
	bHandled = TRUE;
	return TRUE;
}

LRESULT CMainWindow::OnToolsColor(WORD wCode, WORD wId, HWND hWnd, BOOL &bHandled)
{
	bHandled = TRUE;
	return TRUE;
}

LRESULT CMainWindow::OnToolsRedeye(WORD wCode, WORD wId, HWND hWnd, BOOL &bHandled)
{
	bHandled = TRUE;
	return TRUE;
}

LRESULT CMainWindow::OnToolsHistogramEqualize(WORD wCode, WORD wId, HWND hWnd, BOOL &bHandled)
{
	bHandled = TRUE;
	return TRUE;
}

LRESULT CMainWindow::OnToolsFilterErode(WORD wCode, WORD wId, HWND hWnd, BOOL &bHandled)
{
	bHandled = TRUE;
	return TRUE;
}

LRESULT CMainWindow::OnToolsFilterDilate(WORD wCode, WORD wId, HWND hWnd, BOOL &bHandled)
{
	bHandled = TRUE;
	return TRUE;
}

LRESULT CMainWindow::OnToolsFilterBlur(WORD wCode, WORD wId, HWND hWnd, BOOL &bHandled)
{
	bHandled = TRUE;
	return TRUE;
}

LRESULT CMainWindow::OnToolsFilterGaussian(WORD wCode, WORD wId, HWND hWnd, BOOL &bHandled)
{
	bHandled = TRUE;
	return TRUE;
}

LRESULT CMainWindow::OnToolsFilterGradient(WORD wCode, WORD wId, HWND hWnd, BOOL &bHandled)
{
	bHandled = TRUE;
	return TRUE;
}

LRESULT CMainWindow::OnToolsFilterZerocross(WORD wCode, WORD wId, HWND hWnd, BOOL &bHandled)
{
	bHandled = TRUE;
	return TRUE;
}

